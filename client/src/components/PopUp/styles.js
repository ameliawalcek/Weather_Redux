import { makeStyles, fade } from '@material-ui/core/styles'

export default makeStyles(() => ({
    root: {
        background: "#ffffff",
        justifyContent: "center",
        color: "#d0acf8",
        fontSize: '17px',
        fontWeight: '700',
        height: '27px',
        alignContent: 'center'
    }
}))