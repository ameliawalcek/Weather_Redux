import { makeStyles, fade } from '@material-ui/core/styles'

export default makeStyles(() => ({
    root: {
        background: "#ffffff",
        justifyContent: "center",
        color: "#b4c4fc",
        fontSize: '17px',
        fontWeight: '700',
        height: '27px',
        alignContent: 'center'
    }
}))