// import {  } from '../constants/constants'
//5
//true = F degrees
const favorites = (favorites = ['111'], action) => {
    switch (action.type) {

        default: return favorites
    }
}
export default favorites