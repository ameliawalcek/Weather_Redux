import { FAHRENHEIT, CELSIUS } from '../constants/constants'
//5
//true = F degrees
const user = (user = [], action) => {
    switch (action.type) {

        default: return user
    }
}
export default user