import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
    selected: {
        color: 'black'
    },
    notSelected: {
        color: 'blue',
        cursor: 'pointer'
    }
}))